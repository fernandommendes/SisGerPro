@model IEnumerable<SisGerPro.Models.Projetos>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Nome)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Finalizado)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.DtInicio)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.DtFim)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Pessoas.Nome)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Nome)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Finalizado)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.DtInicio)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.DtFim)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Pessoas.Nome)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.id_Projeto }) |
            @Html.ActionLink("Details", "Details", new { id=item.id_Projeto }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.id_Projeto })
        </td>
    </tr>
}

</table>
